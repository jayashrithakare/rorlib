<%= form_for(@ruby) do |f| %>
  <% if @ruby.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ruby.errors.count, "error") %> prohibited this ruby from being saved:</h2>

      <ul>
      <% @ruby.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
